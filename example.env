# Restic Configuration
RESTIC_REPOSITORY=b2:your-bucket-name:backup-path
RESTIC_PASSWORD=your-secure-restic-password

# Backblaze B2 Credentials
B2_ACCOUNT_ID=your-b2-account-id
B2_ACCOUNT_KEY=your-b2-account-key

# Backup Configuration
BACKUP_SOURCE_DIR=/backup
BACKUP_TEMP_DIR=/tmp/backup

# Retention Policy (how many snapshots to keep)
RESTIC_KEEP_DAILY=7
RESTIC_KEEP_WEEKLY=4
RESTIC_KEEP_MONTHLY=6
RESTIC_KEEP_YEARLY=2

# Schedule Configuration (cron format)
# Backup schedule - default: daily at 2 AM
BACKUP_SCHEDULE=0 2 * * *
# Restore check schedule - default: daily at 1 AM (checks if directory is empty and restores if needed)
RESTORE_CHECK_SCHEDULE=0 1 * * *

# Healthcheck Configuration
# How often to perform full B2 connectivity checks (in seconds) - default: 3600 (1 hour)
HEALTHCHECK_FULL_INTERVAL=3600